require 'spec_helper'

describe Mui::Sass do
  it 'has a version number' do
    expect(Mui::Sass::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
